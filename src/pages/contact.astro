---
// Asegúrate de tener tu componente Layout
import { Icon } from "astro-icon/components";
import Layout from "../layouts/Layout.astro";

const title = "Contáctanos";
const description =
  "Ponte en contacto con nuestro equipo para consultas de ventas, soporte o información general.";

// Define la información de contacto de forma centralizada
const contactInfo = {
  email: "comercializadoragive@gmail.com",
  phone: "+57 320 359 3447",
  whatsapp: "+57 320 359 3447",
  address: "Calle 91 A # 94L - 16, Bogotá, Colombia",
};
// Función para crear el enlace de WhatsApp
const whatsappLink = `https://wa.me/${contactInfo.whatsapp.replace(/\s/g, "").replace("+", "")}`;
---

<Layout title={title} description={description}>
  <div class="max-w-4xl mx-auto p-4 sm:p-6 lg:p-8">
    <h1 class="text-3xl font-bold mb-4 text-gray-900">{title}</h1>
    <p class="text-xl text-gray-600 mb-8">
      Estamos listos para ayudarte. Envíanos un mensaje o contáctanos
      directamente.
    </p>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-10">
      <div class="space-y-6">
        <h2 class="text-2xl font-semibold mb-4 text-gray-800">
          Información de Contacto
        </h2>

        {/* WhatsApp */}
        <div class="flex items-start space-x-3 gap-2">
          <a
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="text-gray-600 hover:text-green-500 transition-colors"
          >
            <Icon name="mdi:whatsapp" class="w-6 h-6 text-green-500 mt-1" />
          </a>
          <div>
            <a
              href={whatsappLink}
              target="_blank"
              rel="noopener noreferrer"
              class="text-gray-600 hover:text-green-500 transition-colors"
            >
              <p class="font-medium text-gray-900">WhatsApp</p>
              {contactInfo.whatsapp}
            </a>
          </div>
        </div>

        {/* Email */}
        <div class="flex items-start space-x-3 gap-2">
          <Icon name="mdi:email-outline" class="w-6 h-6 text-primary mt-1" />
          <div>
            <p class="font-medium text-gray-900">Correo Electrónico</p>
            <a
              href={`mailto:${contactInfo.email}`}
              class="text-gray-600 hover:text-primary transition-colors"
            >
              {contactInfo.email}
            </a>
          </div>
        </div>

        {/* Teléfono */}
        <div class="flex items-start space-x-3 gap-2">
          <Icon name="mdi:phone-outline" class="w-6 h-6 text-primary mt-1" />
          <div>
            <p class="font-medium text-gray-900">Teléfono</p>
            <a
              href={`tel:${contactInfo.phone.replace(/\s/g, "")}`}
              class="text-gray-600 hover:text-primary transition-colors"
            >
              {contactInfo.phone}
            </a>
          </div>
        </div>

        {/* Dirección (si aplica) */}
        <div class="flex items-start space-x-3 gap-2">
          <a
            href="https://www.google.com/maps/place/Comercializadora+GIVE/@4.7158014,-74.0981168,17z/data=!4m6!3m5!1s0x8e3f8500679e42fd:0x2dbbc854db0d4341!8m2!3d4.7158072!4d-74.0980596!16s%2Fg%2F11v178p5b5?entry=ttu&g_ep=EgoyMDI1MTAxNC4wIKXMDSoASAFQAw%3D%3D"
          >
            <Icon
              name="mdi:map-marker-outline"
              class="w-6 h-6 text-primary mt-1"
            />
          </a>
          <div>
            <a
              href="https://www.google.com/maps/place/Comercializadora+GIVE/@4.7158014,-74.0981168,17z/data=!4m6!3m5!1s0x8e3f8500679e42fd:0x2dbbc854db0d4341!8m2!3d4.7158072!4d-74.0980596!16s%2Fg%2F11v178p5b5?entry=ttu&g_ep=EgoyMDI1MTAxNC4wIKXMDSoASAFQAw%3D%3D"
            >
              <p class="font-medium text-gray-900">Ubicación</p>
              <p class="text-gray-600 hover:text-primary transition-colors">
                {contactInfo.address}
              </p>
            </a>
          </div>
        </div>

        <h2 class="text-2xl font-semibold pt-4 text-gray-800">
          Horario de Atención
        </h2>
        <p class="text-gray-700">Lunes a Viernes: 9:00 AM - 5:00 PM (COT)</p>
        <p class="text-gray-700">Sábados: 9:00 AM - 12:00 PM (COT)</p>
      </div>

      <div class="bg-white p-6 rounded-xl shadow-lg border border-gray-200">
        <h2 class="text-2xl font-semibold mb-4 text-gray-800">
          Envíanos un Mensaje
        </h2>

        {
          /* 💡 NOTA: Este formulario debe ser conectado a un servicio de backend o 
          a una herramienta externa (como Netlify Forms, Formspree, etc.) 
          para que realmente envíe el correo. El atributo 'action' y 'method' 
          dependerán de tu elección de backend.
        */
        }
        <!-- //TODO: crear esto después, baja prioridad -->
        <form
          name="contact"
          method="POST"
          action="/success"
          data-netlify="true"
          class="space-y-4"
        >
          <input type="hidden" name="form-name" value="contact" />

          <div>
            <label
              for="name"
              class="block text-sm font-medium text-gray-700 mb-1"
              >Nombre Completo</label
            >
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full p-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary"
            />
          </div>

          <div>
            <label
              for="email"
              class="block text-sm font-medium text-gray-700 mb-1"
              >Correo Electrónico</label
            >
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full p-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary"
            />
          </div>

          <div>
            <label
              for="subject"
              class="block text-sm font-medium text-gray-700 mb-1">Asunto</label
            >
            <input
              type="text"
              id="subject"
              name="subject"
              class="w-full p-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary"
            />
          </div>

          <div>
            <label
              for="message"
              class="block text-sm font-medium text-gray-700 mb-1"
              >Mensaje</label
            >
            <textarea
              id="message"
              name="message"
              rows="4"
              required
              class="w-full p-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary"
            ></textarea>
          </div>

          <button
            type="submit"
            class="w-full py-3 bg-primary text-white font-semibold rounded-lg hover:bg-opacity-90 transition-opacity"
          >
            Enviar Mensaje
          </button>
        </form>
      </div>
    </div>
  </div>
</Layout>
